{
  "name": "adam-provider-browser",
  "version": "0.1.0",
  "description": "Local browser-session ADAM provider. Uses a dedicated Chrome profile; SWITCH edu-ID stays in the browser.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "login": "tsx src/login-cli.ts",
    "status": "tsx src/status-cli.ts",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "adam-core": "*",
    "playwright-core": "^1.55.0"
  }
}
